{
  "name": "kubeclaude",
  "version": "0.1.0",
  "private": true,
  "description": "Web UI that schedules and runs Claude Code prompts inside Kubernetes",
  "license": "MIT",
  "workspaces": [
    "server",
    "web",
    "e2e"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "npm run build --workspace web && npm run build --workspace server",
    "dev": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "start": "node server/dist/index.js",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web && npm run typecheck --workspace e2e",
    "test": "npm run test --workspace server && npm run test --workspace web",
    "test:server": "npm run test --workspace server",
    "test:web": "npm run test --workspace web",
    "e2e": "npm run e2e --workspace e2e",
    "verify": "npm run lint && npm run typecheck && npm test && npm run build",
    "lint": "eslint . --max-warnings=0",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "eslint-plugin-react-hooks": "^5.2.0",
    "globals": "^17.8.0",
    "typescript-eslint": "^8.65.0"
  }
}
